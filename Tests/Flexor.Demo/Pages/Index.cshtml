@page "/"


    <FlexLayout>
        <FlexItem Size="Size.IsPercent(50)"></FlexItem>

        <FlexItem Size="Size.IsPixels(50)"></FlexItem>

        <FlexItem Size="Size.IsPercent(50)"></FlexItem>

        <FlexItem Size="Size.IsPixels(50)"></FlexItem>

    </FlexLayout>


@functions {


}
